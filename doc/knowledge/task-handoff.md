# Task Handoff · AI 协作交接约定

> **本约定为永久工作流,适用于所有未来任务。每次会话开始前请先读本文。**

## 规则

每次涉及代码或文档变更的任务,执行结束时必须产出两件事:

### 1. `doc/task/<NNN>-<slug>.md` — 任务执行记录

固定五段结构:
- `## 任务背景` — 一两句话说明这次做了什么、起因
- `## 执行摘要` — 实际产出的关键变更(文件清单 + 简述,不复述每行代码)
- `## 手工测试` — **必含**。命令 + 实测输出 + 结论。无 UI 时记录 curl;有 UI 时记录浏览器步骤与观察结果。诊断到问题(根因、配置、外部依赖)一并写入,即使问题非本次代码引起
- `## 遗留 TODO` — 计划中未完成、被显式延后或新发现的项,带责任域(后端/前端/文档/测试)
- `## 下一阶段建议` — 基于 `doc/prd.md` 推断的合理下一步,3-5 条,每条注明 PRD 章节定位与价值理由

### 2. 同步维护本文件

若本约定本身被调整(例如新增段落、修改文件名规则),需同步更新本文件并在 commit 信息中说明。

## 命名与存放

- 目录:`doc/task/`(注意与 `doc/milestone/` 区分:milestone 是前瞻里程碑计划,task 是回顾执行记录)
- 文件名:**`<NNN>-<kebab-slug>.md`**,其中 `NNN` 是 0 填充的 3 位序号,从 `001` 起递增
  - 示例:`001-foundation-framework.md` → `002-onboarding-flow.md` → `003-scene-select-skill.md`
  - 序号在文件名;**任务执行日期写入文件首行 `> 日期: YYYY-MM-DD`**,与序号解耦
- 一次任务一个文件,不追加;同一任务跨日继续也复用原序号文件,只更新首行日期范围(如 `> 日期: 2026-05-16 ~ 2026-05-18`)
- 创建新文件前先 `ls doc/task/` 取最大序号 +1
- 即使任务被废弃也保留序号占位,避免歧义

## 手工测试章节规范

`## 手工测试` 是 002 起新增的**强约束**,旨在让任何人通过任务文档就能复现验证步骤,不依赖在线对话上下文。

- **粒度**:每步一行命令或一句 UI 操作,后面紧跟输出片段或观察结果(用 ` ``` ` 代码块)
- **命令必须可直接复制粘贴**:**禁止**在命令前加 `$ ` `>` `PS>` 等 shell 提示符前缀。命令块只放命令本身,输出另起代码块或紧跟在命令块之后。多行命令用 `\` 续行(POSIX)或反引号续行(PowerShell),保持单次粘贴可执行
- **覆盖**:本次变更涉及的所有可执行入口至少跑过一次
  - 后端 API:curl / PowerShell `Invoke-RestMethod` 跑关键路由,记录状态码与响应体
  - 前端 UI:浏览器路径(打开 → 点击 → 观察),配截图或文字描述
  - 流式接口:截取 SSE 前 2-3 个事件
  - 失败路径:至少跑 1 个负样本(非法输入、未登录、超时等)
- **诊断**:测试中遇到问题,在「现象 → 诊断 → 根因 → 处置」四段记录。即使问题在外部(网络、第三方 endpoint、配置错误),也要写入,便于复现与排错
- **凭据安全**:实际命令中的 token / API key 用 `<TOKEN>` `<API_KEY>` 占位,不入文档;变量类(如生成的 `<EMAIL>` `<CONV_ID>`)同样用尖括号占位,并在前文说明如何获取
- **不可省略**:即使所有测试通过,也要写"已跑过 X / X 步,全部通过"明示

## 与其他文档的关系

- `doc/prd.md` — 产品需求事实源,下一阶段建议必须引其章节
- `doc/esd.md` — 工程规范事实源,执行约束与 commit 前缀均按 ESD 走
- `doc/milestone/` — 前瞻里程碑计划(尚未落地),与 task/ 是镜像关系(milestone 描述要做什么,task 记录做了什么)
- `doc/knowledge/architecture.md` 等主题文档 — 任务中如果引入新的架构决策,需同步更新对应主题文档

## 触发场景

- 任何新增功能、修复 bug、重构、文档大改的会话结束时
- 不含范围:仅询问 / 仅阅读 / 仅运行命令的会话不需要产出 task 记录

## 模板

```markdown
> 日期: YYYY-MM-DD
> 序号: NNN
> 任务: <一句话标题>

## 任务背景

…

## 执行摘要

- `path/to/file.ext` — 简述
- …

## 手工测试

### 后端 API(curl / PowerShell)

命令(可直接复制粘贴,**无 `$` 前缀**):

\`\`\`bash
curl -X POST http://127.0.0.1:8787/api/xxx -d '...'
\`\`\`

响应:

\`\`\`json
{"data": {...}}
\`\`\`

### 前端 UI(浏览器)

1. 访问 http://localhost:5173/login → 看到登录表单
2. 输入 …,点击「登录」→ 自动跳转到 /chat
3. …

### 诊断记录(若有)

- 现象:…
- 根因:…
- 处置:…

### 总结

已跑过 N / N 步,X 通过 / Y 通过(降级)/ Z 阻断。

## 遗留 TODO

- [后端] …
- [前端] …
- [文档] …
- [测试] …

## 下一阶段建议

1. **<主题>**(PRD §X.Y)— 价值理由 + 落地切入点
2. …
```
